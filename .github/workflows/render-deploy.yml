name: Render Deploy

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_DB: parroquiaDB
          POSTGRES_USER: parroquiaUser
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Set environment variables
      run: |
        echo "DATABASE_URL=postgres://parroquiaUser:password@localhost:5432/parroquiaDB" >> $GITHUB_ENV
        echo "ALLOWED_HOSTS=localhost 127.0.0.1" >> $GITHUB_ENV

    - name: Run tests
      run: |
        find . -path "*/migrations/*.py" -not -name "__init__.py" -delete
        python manage.py makemigrations
        python manage.py migrate
        python manage.py test

    - name: Deploy to Render
      if: success()
      run: |
        curl -X POST -H "Content-Type: application/json" -H "Authorization: Bearer ${{ secrets.RENDER_TOKEN }}" -d '{}' https://api.render.com/v1/services/srv-con2hacf7o1s73fccrdg/deploys
